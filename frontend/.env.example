# 本地可选：直连后端（仅当页面从 localhost 打开时生效；生产构建勿提交含 localhost 的 .env.local）
# NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# Railway「前端」服务：服务端请求后端须走私网（在 Variables 里配置，勿提交真实值）
# BACKEND_URL=http://<backend-service-name>.railway.internal:8000
# 或在 Railway 里用「引用」后端服务的 Private URL / RAILWAY_PRIVATE_DOMAIN 拼端口
# 注意：middleware 使用 Node runtime 才能解析 .railway.internal（见 middleware.ts export const runtime）

# 任务：止盈止损可视化设置功能

## 现状
- 止损止盈逻辑在 `backend/scheduler.py` 的 `check_position_for_exit()` 函数中，硬编码：止损-8%、止盈+15%
- positions 表没有 stop_loss / take_profit 字段
- 前端策略详情页 `frontend/app/strategies/[id]/page.tsx` 没有展示持仓的风控参数
- 没有修改止盈止损的 API

## 需要做的

### 1. 数据库层
- `backend/database.py` 的 SCHEMA_SQL 中 positions 表增加字段：
  - `stop_loss_pct REAL DEFAULT -8.0` （止损百分比，负数，默认-8%）
  - `take_profit_pct REAL DEFAULT 15.0` （止盈百分比，正数，默认15%）
- 兼容已有数据（ALTER TABLE 添加列，对旧数据用默认值）

### 2. 后端 API
- `backend/api/strategies.py` 新增接口：
  - `PUT /api/strategies/{id}/positions/{position_id}/risk-params` — 设置单个持仓的止损止盈
  - `PUT /api/strategies/{id}/risk-params` — 批量设置策略下所有持仓的止损止盈
  - 请求体：`{ "stop_loss_pct": -5.0, "take_profit_pct": 20.0 }`
  - 返回更新后的持仓信息

### 3. 后端逻辑改造
- `scheduler.py` 的 `check_position_for_exit()` 函数：
  - 从 positions 表读取该持仓的 stop_loss_pct 和 take_profit_pct
  - 替换硬编码的 -8 和 15
- `paper_broker.py` 买入时，新持仓使用 config 默认值写入 stop_loss_pct / take_profit_pct

### 4. 前端展示
- `frontend/app/strategies/[id]/page.tsx` 策略详情页的持仓列表：
  - 每个持仓卡片显示：当前盈亏% vs 止损线 vs 止盈线（带颜色标记）
  - 盈亏接近止损线（距离<2%）显示黄色警告
  - 盈亏接近止盈线（距离<2%）显示黄色提示
  - 可点击编辑止损止盈百分比
- 用 Recharts 或纯 CSS 实现一个简单的盈亏区间条：
  `[止损线 --- 当前盈亏% --- 止盈线]`

### 5. 前端交互
- 点击持仓的止损/止盈数字，弹出 inline 编辑（input + 确认/取消）
- 调用 PUT API 更新
- 更新成功后刷新持仓数据

## 约束
- 不改数据库迁移系统，用 ALTER TABLE 兼容
- 前端用现有 Tailwind 样式风格，不引入新 UI 库
- API 需要保留现有的 auth middleware 校验
